<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.jeju.admin.member.list.dao.AdminMemberListDao">

	<!-- 전체 회원 리스트 조회 -->
	<select id="memList" resultType="adminMemList">
		SELECT mem_id,
		mem_name,
		mem_gender, mem_birthday, mem_cellphone,
		mem_state,
		mem_joindate
		FROM member
	</select>

	<!-- 회원 정보 상세 페이지 조회 -->
	<select id="memDetail" resultType="adminMemList">
		SELECT mem_id, mem_name,
		mem_gender, mem_birthday, mem_cellphone,
		mem_state, mem_joindate
		FROM
		member
		WHERE mem_id = #{mem_id}
	</select>

	<!-- 게시물 수정 -->
	<update id="memUpdate" parameterType="adminMemList">
		UPDATE member
		SET
		mem_state = #{mem_state}
		WHERE mem_id = #{mem_id}
	</update>
</mapper>
